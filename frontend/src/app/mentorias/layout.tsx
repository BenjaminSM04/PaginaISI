import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Mentorías' };

export default function MentorshipsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
